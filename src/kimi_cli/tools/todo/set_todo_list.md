Track progress on complex/multi-step tasks.

**Modes:** Update (replace list with `todos`), Query (omit or `null`), Clear (`[]`).

**Rules:** Use only for complex tasks. Pass complete list when updating. Avoid repeated calls without progress; query instead if stuck.
